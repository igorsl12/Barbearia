{
  "permissions": {
    "defaultMode": "bypassPermissions",
    "deny": [
      "Bash(curl * | bash)",
      "Bash(wget * | sh)",
      "Bash(git push --force*)",
      "Bash(rm -rf /*)",
      "Bash(rm -rf ~*)"
    ]
  }
}